<h1><%= @user.name %></h1>

<table class="profile" summary="Profile information">
  <tr>
    <td class="main">
	  <% unless @user.ads.empty? %>
        <table class="ads" summary="User ads">
		  <h2 class="ad">Posted ads</h2>
          <%= render @user_ads %>
        </table>
        <%= will_paginate @user_ads %>
      <% end %>
    </td>
	
  </tr>
</table>
